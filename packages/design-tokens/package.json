{
  "name": "@tm/design-tokens",
  "version": "1.0.0",
  "description": "design token concept using figma as single source of truth",
  "main": "./dist/index.js",
  "sideEffects": false,
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "node ./build.js",
    "clean": "rm -rf dist",
    "prepublishOnly": "yarn build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "style-dictionary": "^3.1.1"
  },
  "publishConfig": {
    "access": "public"
  }
}
